# This is an example config file for repos tied to RHEL 8.6
# Repos not tied to a specific OS release should go in a general config file
# Either copy or rename this file to end with .conf to use it
OS=rhels8.6.0
ARCH=x86_64
EXCLUDES="" #Space separated list of rpms to exclude from syncing
REPO_BASE_DIR=/install/repos
REPO_DIR=$REPO_BASE_DIR/$OS/$ARCH
REPOS=(
# Comment out any repo like this
#"sync-codeready-builder-for-rhel-8.6-x86_64-eus-rpms"
#"sync-rhel-8.6-for-x86_64-appstream-eus-rpms"
#"sync-rhel-8.6-for-x86_64-baseos-eus-rpms"
)
